# secrets/roles/worker.yaml — Worker Role Secrets
#
# Layer: 2 (overrides shared, overridden by host layer)
#
# This file contains secrets specific to the worker role.
# Workers typically need minimal secrets (e.g., cloudflared credentials).
#
# Migration plan: Secrets will be moved here from secrets/frontline-01.yaml
# during Phase 2 of typed-secrets migration.
#
# Currently EMPTY — no secrets migrated yet.
