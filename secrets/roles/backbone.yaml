# secrets/roles/backbone.yaml — Backbone Role Secrets
#
# Layer: 2 (overrides shared, overridden by host layer)
#
# This file contains secrets specific to the backbone role:
# ArgoCD, Forgejo, cloudflared, Harbor, etc.
#
# Migration plan: Secrets will be moved here from secrets/backbone-01.yaml
# during Phase 2 of typed-secrets migration.
#
# Currently EMPTY — no secrets migrated yet.
