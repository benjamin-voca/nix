# secrets/shared.yaml — Cluster-Wide Shared Secrets
#
# Layer: 1 (lowest priority — overridden by role and host layers)
#
# This file contains secrets shared across all machines in the cluster.
# Examples: Harbor admin password, Ceph S3 keys, etc.
#
# Migration plan: Secrets will be moved here from secrets/backbone-01.yaml
# during Phase 2 of typed-secrets migration.
#
# Currently EMPTY — no secrets migrated yet.
