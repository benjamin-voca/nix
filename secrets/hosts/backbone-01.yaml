# secrets/hosts/backbone-01.yaml — Machine-Specific Overrides
#
# Layer: 3 (highest priority — overrides shared and role layers)
#
# This file contains secrets specific to backbone-01 that differ from
# the backbone role defaults. Examples: machine-specific API keys,
# per-host cloudflared credentials, etc.
#
# Migration plan: Machine-specific secrets will be moved here during
# Phase 2 of typed-secrets migration.
#
# Currently EMPTY — no secrets migrated yet.
